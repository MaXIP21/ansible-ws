- name: Clone a foreman repository.
  git:
    repo: git clone --recursive git://github.com/theforeman/foreman-installer.git -b develop
    version: master
    dest: /home/ansible/foreman-install
    accept_hostkey: yes
  become: no
